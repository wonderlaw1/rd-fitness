import { CreditCardLogoDirective } from './credit-card-logo.directive';

describe('CreditCardLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditCardLogoDirective();
    expect(directive).toBeTruthy();
  });
});
